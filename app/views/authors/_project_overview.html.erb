<% if @project.author.present? %>

    <div class="members box" >
      <h3>Author</h3>

      <div >
        <div class="author_view" >
          <%=avatar @project.author.user, :size => "90"%>
          <h3><%= link_to @project.author.user, @project.author.user %></h3>
          <h4><%=@project.author.user.email_address.address%> </h4>
        </div>
      </div>

    </div>

<% end %>